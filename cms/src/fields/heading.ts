import { TextField } from "payload/types";

export const headingField: TextField = {
  name: "heading",
  label: {
    en: "Heading",
    es: "Título",
  },
  type: "text",
  required: true,
  localized: true,
};
